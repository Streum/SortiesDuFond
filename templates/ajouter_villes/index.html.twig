{% extends 'base.html.twig' %}

{% block title %}Villes index{% endblock %}

{% block body %}
    <h1>Villes index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomVille</th>
                <th>CodePostal</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ville in villes %}
            <tr>
                <td>{{ ville.id }}</td>
                <td>{{ ville.nomVille }}</td>
                <td>{{ ville.codePostal }}</td>
                <td>
                    <a href="{{ path('app_ajouter_villes_show', {'id': ville.id}) }}">show</a>
                    <a href="{{ path('app_ajouter_villes_edit', {'id': ville.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_ajouter_villes_new') }}">Create new</a>
{% endblock %}
