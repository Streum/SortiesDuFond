{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="d-flex justify-content-center">
    <button class="btn btn-success ms-2">{{ button_label|default('Sauvegarder') }}</button>
</div>
{{ form_end(form) }}