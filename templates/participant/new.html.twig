{% extends 'base.html.twig' %}

{% block title %}Nouveau Utilisateur{% endblock %}

{% block body %}
    <h1 class="d-flex justify-content-center">Créer un utilisateur</h1>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-4">
    {{ include('participant/_form.html.twig') }}
        </div>
    </div>
</div>

<div class="d-flex justify-content-center">
    <a href="{{ path('app_participant_index') }}" class="btn btn-primary">Liste des utilisateurs</a>
</div>
{% endblock %}
