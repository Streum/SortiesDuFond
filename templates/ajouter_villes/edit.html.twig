{% extends 'base.html.twig' %}

{% block title %}Edit Villes{% endblock %}

{% block body %}
    <h1>Edit Villes</h1>

    {{ include('ajouter_villes/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_ajouter_villes_index') }}">back to list</a>

    {{ include('ajouter_villes/_delete_form.html.twig') }}
{% endblock %}
