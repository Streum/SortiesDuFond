{% extends 'base.html.twig' %}

{% block title %}Importer Utilisateurs{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-center mb-4">Importer des Utilisateurs à partir d'un fichier CSV</h1>

        <div class="card shadow-m">
            <div class="card-body">
                {{ form_start(form) }}
                <div class="mb-3">
                    {{ form_label(form) }}
                    {{ form_widget(form) }}
                    {{ form_errors(form) }}
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
